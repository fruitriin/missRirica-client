---
name: addf-dev
description: |
  TODO.md から未実施タスクを1つ選んで実装・品質検証・コミットまで完遂する。
  1タスクだけ実施: `/addf-dev`。繰り返し自律実行: `/loop 1h /addf-dev`。
  タスクを進めたいとき、バックログを消化したいときに使う。
user_invocable: true
---

# addf-dev — 1タスク実施

## 手順

### 1. コンテキスト読み込み

以下を順番に読む:

1. `.claude/addf/Feedback.md` — 未対応の改善アクションを確認
2. `TODO.md` — 未着手タスクを列挙
3. `.claude/addf/Progress.md` — 進行中タスクがあれば継続、なければ次へ

### 2. タスク選択

TODO.md の「未着手」タスクを対象に、以下の優先順位で1つ選ぶ:

**優先1 — 複利効果**: 他タスクのブロッカー解消・インフラ整備・テスト基盤・ツール改善など、
実施することで後続タスクが楽になるもの。

**優先2 — 若番**: 依存関係がない場合、計画ファイルの番号が小さいもの
（例: `phase21.4` < `phase22.1`、`0026` < `0028`。命名はプロジェクトによる）。

選んだタスクを一行で示す: `→ <計画ファイル名> を実施します`

**アイドル時（選べるタスクがない場合）**: 未着手がない、または残る未着手が全て「要確認（質問投下済み）」
「オーナー指示待ち」のときは、`addf-Behavior.toml` の `[speculation].enable` が true なら
`/addf-speculate` を1サイクル実行して手順4（完了処理）へ進む。false なら従来どおりオーナーに確認する。

### 3. 実装

`.claude/addf/plans/` から選んだ計画ファイルを読み、CLAUDE.md のブートシーケンスに従って実装する
（`CLAUDE.repo.md` でプランディレクトリが追加定義されている場合はそちらも対象）。
`.claude/addf/Progress.md` の運用ルールに従い、**2段階品質ゲート**（Stage 1: ビルド・Lint・テスト → Stage 2: quality-gate チーム）を完遂する。
Stage 1/Stage 2 の構成はダウンストリームの `.claude/addf/Progress.md` 運用ルール側が正 — 独自のフェーズ構成を持つプロジェクトでは Progress.md の定義に読み替える。Stage 分割が無い単線構成では全体を単一の品質ゲートとして扱う（デフォルト ProgressTemplate ではステップ4=ビルド・Lint・テスト、ステップ5〜7=レビューと指摘対応が相当）。

**日記（代替わり引き継ぎ）**: サブタスク完了時・重要な判断の直後・計画変更時に、Progress.md のタスク「#### 日記」へ4項目エントリー（やったこと/今の見立て/次の自分へ/気になっていること）を書く。書式は Progress.md 運用ルール 3.5 参照。`/loop` の次イテレーションの自分が最初に読む引き継ぎ文書になる。

**迷ったとき（閾値割れ）**: CLAUDE.md「迷ったときの作法（7割共有原則）」に従う。

- 確信度が閾値（デフォルト7割）を下回り responsiveness が `relaxed` の場合: `.claude/addf/Questions.md` に質問を置き、TODO の該当タスクを「要確認（質問投下済み）」にして**次のタスクを選び直す**（ステップ2へ戻る）
- `unattended` の場合: 質問を置いた上で最有力解釈により投機続行し、作業を `speculative/` ブランチに隔離する
- worktree 隔離下で実装している場合は閾値を1段下げてよい（失敗を捨てられるため）

**変更ルート判断**: 新規変更・フォローアップの経路（dev 直行 / オーナー問い合わせ / speculate 方式）は変更の性質で決める。判断表と適用順序は [`.claude/addf/guides/speculative-development.md`](../addf/guides/speculative-development.md) の「変更ルート判断」節が単一ソース。dev 直行が原則で、外部契約変更や利用者影響が読めない場合はオーナー問い合わせ、GUI 変更や見せないと判断できないものは speculate 方式を選ぶ。

**委譲時の禁止事項**: `Agent` tool 経由でサブエージェント（worktree 実装等）に委譲するときは、共通禁止事項テンプレート [`@.claude/addf/templates/DelegationRules.md`](../addf/templates/DelegationRules.md) をプロンプトに含める。Progress.md の境界（タスク欄 vs 運用ルール節）・git 操作・単一ソース・スコープ・ノウハウ記録の5項目が単一ソース化されている。プロジェクト固有の追加ルールは同ファイルの末尾「プロジェクト固有ルール」節に追記する。

### 4. 完了処理

`.claude/addf/Progress.md` の運用ルールのうち「ノウハウ蓄積」「フィードバック記録」「アーカイブとコミット」（ステップ 9〜15）を全て実行し、コミットする。

PR を作成する場合は、本文を `.claude/addf/guides/pr-format.md` の規約（対象 Plan リンク・計画の進捗位置）に従って書く
（PR を作るかどうかの判断基準は Plan 0035 フェーズB で規定予定。現行は通常コミット直行）。

### 5. ループ継続

`/loop` が次の実行を自動スケジュールするため、1サイクルの処理を終えたら完了とする。
次のサイクルで再びステップ1から再開される。

**コンテキスト満杯時の出口**（Plan 0041）: コンテキスト残量が少ないことを理由にループを止めない・タスク着手を控えない。auto-compact は harness が上限接近時に自動発動し、`post-compact-recovery.sh` と日記が受け止める。エージェントの仕事は止まらないこと。

- 残量少時は**復帰容易性の高いタスクを優先**する: 進捗がファイル差分（コミット・Progress.md チェックリスト・日記）に現れるタスク・サブタスクの刻みが小さいタスク。compaction を跨いでも次の代がファイル差分と圧縮済みコンテキストだけで続きから入れる
- 逆に**未コミットの大きな途中状態を長時間抱える one-shot 級タスクは残量少時に着手しない**（compaction で失うものが大きい）
- 進捗の外部化（こまめなコミット・チェックリスト更新・日記）を通常より密に刻む

## 経験の活用
- 実行前に `addf-dev.exp.md` が存在すれば読み、過去の経験（タスク選択判断、よくあるトラブル等）を考慮する
- 実行後、新たな教訓があれば `addf-dev.exp.md` に追記する
