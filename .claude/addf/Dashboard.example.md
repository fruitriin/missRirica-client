# 前回セッションからの差分まとめ

unattended 自走中に `dashboard_report: true` の場合、エージェントがこの書式で `.claude/addf/Dashboard.md` を生成する。
次セッション開始時（ブートシーケンス 1.6）に冒頭で提示し、**オーナーの応答を確認してから**削除する（確認前にセッションが終わったら次回再提示）。
時刻メタファー（「おはようございます」等）は使わない — ユーザーの生活時間を仮定せず、中立に「前回からの差分」だけを書く。

## 完了

- <完了した Plan / サブタスクの一覧>

## 投機ブランチ（採否判断待ち）

- `speculative/<phase>-<名前>` — <どの解釈で進めたかの一行説明>

（承認後は `.claude/commands/addf-speculate.md` の「昇格手順」を実施する）

## 質問（未回答）

- <Questions.md の未回答質問の要約と件数>

## 気になった点

- <差し戻し・テスト失敗・鮮度低下など、オーナーに知らせる価値のある観察>
