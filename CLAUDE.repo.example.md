# CLAUDE.repo.md

<!-- このファイルはダウンストリームプロジェクトへのテンプレートであると同時に、
     CLAUDE.repo.md から @メンションで展開されるため、アップストリーム（ADDF本体）
     でもそのまま使われる相互参照パターンです。 -->

AutomatonDevDrive Framework です
このリポジトリはAI駆動自動推進フレームワークです。


# 特徴

- ノウハウ蓄積
- /loop による自己推進システム
- スキルから経験の分離
- GUIテスト内蔵（オプション）

# プロジェクト種別

このリポジトリは **ADDF 開発プロジェクト**（フレームワーク本体）です。
`/addf-permission-audit` はこの宣言に基づき、権限を ADDF 開発プロジェクトパターンで分類します:
- アップストリーム権限 → `settings.local.json`（ADDF 開発固有、ダウンストリームに配布しない）
- ダウンストリーム・汎用権限 → `settings.json`（テンプレートとして配布）

ダウンストリームプロジェクトではこのセクションを以下に書き換えてください:
```
このリポジトリは **ADDF 利用プロジェクト** です。
```

# 哲学
AutomatonDevDrive由来のソースコードは .claude 配下に収めてください。
AutomatonDevDrive由来のスキルは addf- プレフィックスを持ちます
プロジェクトルート配下にADD由来のファイルをなるべく置かないべきです。

---

## コミットログ規約

日本語で書く。形式:

```
[領域] 変更内容の要約

詳細説明（必要な場合）
```

---

## テスト

ADD フレームワーク自体のテストランナー:

```bash
bash .claude/addf/tests/run-all.sh
```

- **フックテスト** (`.claude/addf/tests/hooks/`): ターンカウンター・リセットの自動検証
- **ツールテスト** (`.claude/addf/tests/tools/`): addfTools バイナリの疎通確認
- **スキルテスト** (`.claude/addf/tests/skills/`): 自然言語シナリオ（手動実行）

品質ゲートの Stage 1 でプロジェクト固有のビルド・Lint・テストに加えて、`bash .claude/addf/tests/run-all.sh` を実行してください。

---

## ノウハウ記録の3観点

タスク実行中、以下の3つのタイミングで `/addf-knowhow` を使い知見を記録する。
ProgressTemplate の運用ルールに組み込み済み。

1. **コーディング知見**（実装フェーズ完了時）: 再利用可能なパターン、落とし穴、技術的判断とその根拠
2. **品質ゲート知見**（品質検証完了時）: レビューエージェントが検出した再発しうるパターン（セキュリティ、コード品質、分離パターン違反等）
3. **タスク総括**（完了処理時）: 計画と実装のギャップ、想定外だった点、次回同種タスクへの教訓

記録済みの知見と重複しないよう、各タイミングで既存 knowhow を確認すること。

---

## モデル配分ポリシー（オプション）

複数の Claude モデルを役割別に使い分ける場合、このプロジェクトでの評価と配分方針をここに書く。
考え方・実現手段（エージェント定義の `model:` frontmatter / メインセッションの `/model` /
`Agent` tool 呼び出しの `model` パラメータ）・エイリアスとフル ID ピン留めの使い分けは
[`.claude/addf/guides/model-allocation.md`](.claude/addf/guides/model-allocation.md) を参照。

書式の例（プロジェクトで使えるモデルと評価に応じて書き換える）:

```
<モデルA>: <得意な軸（例: 推論の深さ・美的感覚等）> / <苦手な軸> / <割り当てる役割>
<モデルB>: <得意な軸> / <苦手な軸> / <割り当てる役割>
```

役割の割り当て表（例）:

| 役割 | モデル | 理由 |
|---|---|---|
| メインループ運転手（判断・選択・検収・オーナー対話） | | |
| 実装サブエージェント（`addf-implementer` 等） | | |
| レビュー（通常・skeptic/attacker ペルソナ） | | |
| レビュー（newcomer ペルソナ）・UI/文言・ドキュメント | | |
| Plan 起草・難所 | | |

エスカレーション基準（実装が難航したときの逃し方）や7割共有原則の補正が、
プロジェクト固有の運用と異なる場合はここに追記する。

---

## 品質ゲート拡張（オプション）

プロジェクトの規模やセキュリティ要件に応じて、タスク完了時の品質検証を2段階に分割できます。
この設定を有効にする場合、`.claude/addf/templates/ProgressTemplate.md` の「タスク完了時 — 品質検証」を以下に差し替えてください。

### Stage 1: ビルド検証（ゲートキーパー）

4. プロジェクトのビルド・Lint・テストコマンドを実行する
   - **失敗した場合 → 実装に差し戻す**。原因分析 → 修正 → Stage 1 を再実行
   - Stage 1 を通過するまで Stage 2 に進まない

### Stage 2: 品質検証チーム（並列実行）

5. `quality-gate` チーム（TeamCreate）を作成し、以下のエージェントを **並列** で起動する:

   **[addf-security-review-agent]** — セキュリティレビュー
   - 変更差分に対して脆弱性の有無を検査し、修正案を提示する（実装はしない）
   - 発見した脆弱性を重要度付き（Critical/High/Medium/Low/Info）で報告する

   **[addf-ui-test-agent]** — UI テスト（GUI プロジェクトの場合）
   - 変更に関連するテストシナリオを実行する

   **[addf-code-review-agent]** — コードレビュー
   - コード品質・可読性・ベストプラクティスの観点からフィードバック
   - マイルストーン・リリース直前・`mode: critical` 宣言時・unattended 自走時（`/addf-mode unattended`）は、**ペルソナ並列（視点ずらしレビュー）** で起動する（マイルストーンは3体、`mode: critical` は5体）。
     ペルソナ定義（skeptic / attacker / newcomer / maintainer / domain-skeptic）と集約ルールは `.claude/agents/addf-code-review-agent.md` を参照

6. **Stage 2 の制御フロー**:
   - 全エージェントを **並列** で開始する
   - セキュリティレビューの指摘 → 実装エージェント（`addf-implementer` 等。モデル配分は
     上記「モデル配分ポリシー」節に従う）に修正を移譲する
   - **セキュリティ修正が全て完了するまでフェーズの完了コミットを行わない**

7. フィードバックの集約は **`.claude/addf/templates/ProgressTemplate.addf.md` の運用ルール7** に従う
   （一次軸: 主題との関係 / 二次軸: クリティカル度）。主題内は本フェーズで対応、
   主題外は別 Plan に切り出す。修正後は Stage 1 を再実行して通過を確認する

